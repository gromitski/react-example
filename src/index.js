// Import all required scripts.
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Import global styles (webpack takes care of it).
import './global.css';

ReactDOM.render(<App />, document.getElementById('root'));
